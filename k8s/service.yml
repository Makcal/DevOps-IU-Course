apiVersion: v1

kind: Service
metadata:
  name: app-python-service

spec:
  type: NodePort

  selector:
    app: app-python

  ports:
    - protocol: TCP
      port: 8000
      targetPort: app-http
      nodePort: 30080
