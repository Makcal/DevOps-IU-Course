apiVersion: argoproj.io/v1alpha1

kind: Application
metadata:
  name: my-python-app
  namespace: argocd
  labels:
    app: my-python-app
    environment: all

spec:
  project: default
  source:
    repoURL: https://github.com/Makcal/DevOps-IU-Course.git
    targetRevision: lab13
    path: k8s/mychart
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    # Manual initial sync (no automated block)
